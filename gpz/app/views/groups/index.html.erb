<h1>Список Групп</h1>

<table>
  <tr>
    <th>Название</th>
    <th>Кафедра</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @groups.each do |group| %>
  <tr>
    <td><%= group.number %></td>
    <td><%= group.chair_id %></td>
    <td><%= link_to 'Показать', group %></td>
    <td><%= link_to 'Изменить', edit_group_path(group) %></td>
    <td><%= link_to 'Удалить', group, :confirm => 'Точно?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Новая Группа', new_group_path %>
