<h1>Список кафедр</h1>

<table>
  <tr>
    <th>Название</th>
    <th>Номер</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @chairs.each do |chair| %>
  <tr>
    <td><%= chair.name %></td>
    <td><%= chair.number %></td>
    <td><%= link_to 'Показать', chair %></td>
    <td><%= link_to 'Изменить', edit_chair_path(chair) %></td>
    <td><%= link_to 'Удалить', chair, :confirm => 'Точно?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Новая кафедра', new_chair_path %>
