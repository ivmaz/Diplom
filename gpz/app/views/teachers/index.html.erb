<h1>Список Преподавателей</h1>

<table>
  <tr>
    <th>Имя</th>
    <th>Отчество</th>
    <th>Фамилия</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @teachers.each do |teacher| %>
  <tr>
    <td><%= teacher.name %></td>
    <td><%= teacher.pathname %></td>
    <td><%= teacher.surname %></td>
    <td><%= link_to 'Показать', teacher %></td>
    <td><%= link_to 'Изменить', edit_teacher_path(teacher) %></td>
    <td><%= link_to 'Удалить', teacher, :confirm => 'Точно?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Новый Препродаватель', new_teacher_path %>
